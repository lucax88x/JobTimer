var JobTimer;
(function (JobTimer) {
    var Timer;
    (function (Timer) {
        angular.module('timer', ["angular.circular-slider"]);
    })(Timer = JobTimer.Timer || (JobTimer.Timer = {}));
})(JobTimer || (JobTimer = {}));
